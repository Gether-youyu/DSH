# <DSH永不眠>功能清单(飞书 + 邮件)

版本:桥 v5 / 插件 v14 | 更新:2026-08-20

---

## 一、入口

| 通道 | 使用方式 | 说明 |
|---|---|---|
| **飞书(主)** | 手机飞书 -> 搜索你创建的 DSH 机器人 -> 私聊 | 实时,支持全部指令 |
| **邮件** | 发邮件到配置的邮箱,正文即内容 | 已内置,当前默认未启用;飞书为主通道 |
| **每日推送** | 每天 19:59 自动发邮件 | 当天工作总结,无需操作 |

---

## 二、指令清单(飞书)

### 1. 任务切换

**指令**:`任务列表`(或 `列表` / `任务` / `list`)

**回复样式**:
```
📋 DSH 任务列表(共 7 个,按最近使用排序)
1. [DSH永不眠] 今天的工作(刚刚) ←当前
2. [DSH永不眠] 视频脚本(2小时前)
3. [Documents] 报销整理(昨天 21:30)
...

发「数字」切换任务 · 发「跟随」自动进入电脑最近活跃对话
```

**指令**:`3`(直接发数字,或 `切换 3` / `switch 3`)

**回复样式**:
```
📌 已切换到任务：[DSH永不眠] 今天的工作
之后手机发的消息都会进这个任务，发送「任务列表」可选择其他任务。
```

### 2. 消息去向控制

**指令**:`跟随`(或 `跟随电脑` / `auto`)
**回复**:`🖥️ 已恢复跟随：消息将进入电脑上最近活跃的任务（[DSH永不眠] 今天的工作）。`

**指令**:`当前`(或 `状态`)
**回复**(固定模式):`当前固定任务：[DSH永不眠] 今天的工作`
**回复**(跟随模式):`跟随电脑模式，最近活跃任务：[DSH永不眠] 今天的工作`

### 3. 选择模型

**指令**:`选择模型`(或 `模型选择` / `模型` / `model`)

**回复样式**(先回"正在获取模型列表...",随后):
```
🤖 模型列表:
当前: deepseek-v4-flash 强度:Max

1. DeepSeek-V4-Flash
2. DeepSeek-V4-Pro

请输入序号选择模型
推理强度可选: off / high / max
不选强度则使用系统默认强度:Max
```

**指令**:`1` 或 `2 max`(序号,可带强度)

**回复**:`✅ 已切换到模型: DeepSeek-V4-Pro 强度:Max
当前对话已立即生效。`

> 推理强度说明:DSH 的 DeepSeek 适配器支持三档——`off`(关闭思考)/ `high` / `max`,系统默认 `max`。不选强度则用默认。

### 4. 任务控制(执行中)

**触发**:任务执行 30 秒后仍无输出,自动回复一次:
```
正在猛烈干活中……
输入「停」我会立即终止这次任务，输入「继续」我会执行最近一次被终止的任务，我会在本次任务结束后回复你其他消息。
```

**指令**:`停`(或 `停止`)
**回复**(任务执行中):`⏹ 已终止当前任务。
输入「继续」可重新执行最近一次被终止的任务。`
**回复**(空闲时):`当前没有正在执行的任务。`

**指令**:`继续`
**回复**:`🔄 已恢复执行被终止的任务：<任务内容前60字>…`(任务完成后,同一条消息追加执行结果)
**回复**(无记录):`没有已终止的任务可恢复。`

> 忙时发的普通消息会排队,当前任务结束后自动补处理并回复。

### 5. 普通对话

直接发任意内容 → 注入当前任务(PC 端同一会话,带 `[来自飞书]` 前缀),回复发回飞书,PC 端同步可见。

---

## 三、每日邮件推送

- **时间**:每天 19:59
- **标题**:`DSH提醒 2026-08-18`(DSH提醒+日期)
- **内容**:
  - 直接统计会话文件当天活动(只解析时间/轮次元数据,不读对话正文)
  - 当天 0 点至执行前的活动总结,DeepSeek 生成,生动有情绪,会夸奖
  - 活动范围取当天最早~最晚事件时间;活跃时长按事件间隔累加(>30 分钟计休息),超 6 小时或仍在工作 → 提醒放松、早休息
- **样式示例**:
```
📋 DSH 今日总结(2026-08-18)

今天的你像开了外挂一样……(总结正文)

——
今日活动记录:
- [05:52-16:02] DSH永不眠(105轮)
活跃时长: 8.2 小时
```

---

## 四、技术架构(供维护)

```
手机飞书 -> 飞书机器人(长连接,SDK) -> feishu-bridge.js(命令/状态机/内置定时调度)
手机邮件 -> QQ邮箱 IMAP/SMTP -> bridge.js
        ↘ 队列 bridge/in/*.json ↗
        -> mailbridge 插件(DSH 进程内,v14)
          · 注入 PC 当前活跃会话 / 指定任务(targetSession)
          · 命令处理:task-list/task-select/model-list/model-select
          · 忙时 30 秒提示、停/继续、消息排队补处理
        -> 回复写回队列 -> 桥发送(防重发保护) -> 飞书/邮件
```

关键文件:
- `feishu-bridge.js` - 飞书桥(命令识别、单实例端口锁、内置定时调度、防重发,日志 /tmp/feishu-bridge.log)
- `bridge.js` - 邮件桥
- `mailbridge-plugin.js` - DSH 插件(持久化,~/.dsh/profiles/web/cordis.patch.yml 注册)
- `daily-summary.js` - 每日推送(桥内置调度,每日 19:59;模型与密钥走 DSH 设置)
- `monitor.js` - 心跳/积压监控(桥内置调度,每分钟)
- `bridge/state.json` - 会话映射/排队状态
- `TROUBLESHOOTING.md` - 排障手册
